(function(){const V='25';const parts=Array.from({length:8},(_,i)=>`app-part-${String(i+1).padStart(2,'0')}.txt?v=${V}`);const get=u=>fetch(u,{cache:'no-store'}).then(r=>{if(!r.ok)throw new Error('Falha ao carregar '+u);return r.text()});function decodePart(text){let s=String(text??'').replace(/^\uFEFF/,'').replace(/[\s\u200B\u200C\u200D]/g,'').replace(/-/g,'+').replace(/_/g,'/').replace(/=/g,'');if(!/^[A-Za-z0-9+/]*$/.test(s))throw new Error('Conteúdo inválido no pacote da aplicação.');const rem=s.length%4;if(rem===1)throw new Error('Pacote da aplicação corrompido (Base64).');if(rem)s+='='.repeat(4-rem);const bin=atob(s),out=new Uint8Array(bin.length);for(let i=0;i<bin.length;i++)out[i]=bin.charCodeAt(i);return out}function joinBytes(list){const n=list.reduce((a,b)=>a+b.length,0),out=new Uint8Array(n);let p=0;for(const b of list){out.set(b,p);p+=b.length}return out}Promise.all([Promise.all(parts.map(get)),get(`cloud-boot.js?v=${V}`)]).then(async([chunks,cloud])=>{const decoded=chunks.map(decodePart);const raw=joinBytes(decoded);if(!('DecompressionStream'in window))throw new Error('Navegador sem suporte a DecompressionStream');const ds=new DecompressionStream('gzip');const buf=await new Response(new Blob([raw]).stream().pipeThrough(ds)).arrayBuffer();const code=new TextDecoder().decode(buf);if(!code.trim())throw new Error('Pacote da aplicação vazio.');new Function(code+'\n;'+cloud)()}).catch(err=>{console.error(err);document.getElementById('app').innerHTML=`<main class="auth-page"><section class="auth-card terminal"><div class="eyebrow">MUTAGENÊSES // ERRO DE INICIALIZAÇÃO</div><h1>Falha ao abrir o terminal.</h1><p class="muted">${String(err.message||err)}</p><p class="muted" style="font-size:12px">Versão ${V}. Recarregue com Ctrl+F5 se esta mensagem aparecer depois de uma atualização.</p></section></main>`})})();