(function(){const parts=Array.from({length:8},(_,i)=>`app-part-${String(i+1).padStart(2,'0')}.txt`);Promise.all(parts.map(p=>fetch(p).then(r=>{if(!r.ok)throw new Error('Falha ao carregar '+p);return r.text()}))).then(async chunks=>{const b64=chunks.join(''),raw=Uint8Array.from(atob(b64),c=>c.charCodeAt(0));if(!('DecompressionStream'in window))throw new Error('Navegador sem suporte a DecompressionStream');const ds=new DecompressionStream('gzip'),buf=await new Response(new Blob([raw]).stream().pipeThrough(ds)).arrayBuffer(),code=new TextDecoder().decode(buf);new Function(code)()}).catch(err=>{console.error(err);document.getElementById('app').innerHTML=`<main class="auth-shell"><section class="auth-card"><div class="eyebrow">MUTAGENÊSES // ERRO DE INICIALIZAÇÃO</div><h1>Falha ao abrir o terminal.</h1><p class="muted">${String(err.message||err)}</p></section></main>`})})();