(function(){const V='23';const parts=Array.from({length:8},(_,i)=>`app-part-${String(i+1).padStart(2,'0')}.txt?v=${V}`);const get=u=>fetch(u,{cache:'no-store'}).then(r=>{if(!r.ok)throw new Error('Falha ao carregar '+u);return r.text()});Promise.all([Promise.all(parts.map(get)),get(`cloud-boot.js?v=${V}`)]).then(async([chunks,cloud])=>{const b64=chunks.join(''),raw=Uint8Array.from(atob(b64),c=>c.charCodeAt(0));if(!('DecompressionStream'in window))throw new Error('Navegador sem suporte a DecompressionStream');const ds=new DecompressionStream('gzip');const buf=await new Response(new Blob([raw]).stream().pipeThrough(ds)).arrayBuffer();const code=new TextDecoder().decode(buf);new Function(code+'\n;'+cloud)()}).catch(err=>{console.error(err);document.getElementById('app').innerHTML=`<main class="auth-page"><section class="auth-card terminal"><div class="eyebrow">MUTAGENÊSES // ERRO DE INICIALIZAÇÃO</div><h1>Falha ao abrir o terminal.</h1><p class="muted">${String(err.message||err)}</p></section></main>`})})();